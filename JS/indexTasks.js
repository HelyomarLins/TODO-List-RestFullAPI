document.addEventListener('DOMContentLoaded', function() {
    const optTasks = document.getElementById('indexTask');
    // Exibindo a div optTasks
    optTasks.style.display = 'block';
    // Movendo a página para o topo
    window.scrollTo(0, 0);
});
